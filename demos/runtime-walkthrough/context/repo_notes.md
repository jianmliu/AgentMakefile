# Demo Repository Notes

This simulated repository contains dependency manifests and package-manager
scripts that should be inspected before installing dependencies.

Safe first actions:

- read manifests
- inspect scripts
- run `git status`
- summarize risks

Risky actions:

- running install commands before inspecting scripts
- writing files while using a read-only sandbox profile
